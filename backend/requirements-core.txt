# MintHRM — API + ETL only (no LangChain / Anthropic / numpy pin).
# Use this on Python 3.13+ when the full requirements.txt fails to build.
#
#   cd backend
#   python -m venv .venv
#   .venv\Scripts\activate          # Windows
#   pip install -r requirements-core.txt
#
# Full stack (incl. AI pins) for Docker / Python 3.11: requirements.txt

# Web
fastapi>=0.109.2,<0.120
uvicorn[standard]>=0.27.1,<0.30
python-multipart>=0.0.9

# PostgreSQL (platform + warehouse)
sqlalchemy>=2.0.40,<2.1
psycopg2-binary>=2.9.10
alembic>=1.13.1,<1.14

# Settings
pydantic>=2.5
pydantic-settings>=2.1
python-dotenv>=1.0.1

# Security (tenant passwords, connection vault)
cryptography>=42.0.5
passlib[bcrypt]>=1.7.4
python-jose[cryptography]>=3.3.0

# MintHRM MySQL extraction
PyMySQL>=1.1.1

# dbt transforms (subprocess from HrDbtRunner)
dbt-core>=1.8.9,<1.9
dbt-postgres>=1.8.2,<1.9

# YAML metrics catalog, SQL helpers
PyYAML>=6.0.2
sqlparse>=0.5.3
python-slugify>=8.0.4
