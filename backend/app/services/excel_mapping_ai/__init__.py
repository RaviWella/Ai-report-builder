"""AI chat that finishes mapping the spreadsheet columns an Excel upload's
local-fuzzy-then-AI one-shot pass (`AIService.map_excel`) couldn't resolve —
a small sibling of `rule_report_ai`, same Claude Code chat technique, much
smaller surface (no dynamic spec, no datamart tools, no attachments)."""
